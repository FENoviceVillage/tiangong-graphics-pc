import React from 'react'
import styles from './index.module.scss'

interface Props {}

function PageHeader(props: Props) {
  return (
    <div className={styles.header}>index</div>
  )
}

export default PageHeader
